apiVersion: v1
kind: Service
metadata:
  name: {{ include "7daysdie.fullname" . }}
  labels:
{{ include "7daysdie.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "7daysdie.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
