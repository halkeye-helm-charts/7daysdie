apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "7daysdie.fullname" . }}-test-connection"
  labels:
{{ include "7daysdie.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "7daysdie.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
